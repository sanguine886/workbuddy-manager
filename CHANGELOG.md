# 更新日志

本项目的所有重要变更都会记录在此文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [未发布]

### 新增
- **兼容 Anthropic Messages API（`/v1/messages`）**：Claude Code / Cursor / Cline /
  Zed 等**只认 Anthropic 协议**的客户端现在可以直接接入本网关。

  此前它们用不了——不是配置问题，而是协议不同：`system` 是顶层字段（不在 messages
  里）、`max_tokens` **必填**、`content` 是 block 数组、流式是带 `event:` 行的事件流。
  光靠改 `ANTHROPIC_BASE_URL` 指过来是不通的。

  - 令牌两种传法都认：`x-api-key`（Anthropic SDK）与 `Authorization: Bearer`（Claude Code）
  - **鉴权、IP 管控、配额、限流、日志与用量记账全部复用网关既有实现**，不另起一套
  - 流式按规范逐事件转换：`message_start` → `content_block_start` →若干
    `content_block_delta` → `content_block_stop` → `message_delta` → `message_stop`
  - 工具调用双向映射：OpenAI 的**分片** `arguments` 字符串 ↔ Anthropic 的
    `input_json_delta` 事件；非流式的 `input` 由字符串还原为对象
  - 工具结果的消息形态差异也一并处理：Anthropic 把它放在 user 消息的 block 里，
    OpenAI 要求是独立的 `role: tool` 消息（转换时会拆开并前置）
  - `/v1/models` 按 `anthropic-version` 请求头分流为 Anthropic 形状
    （两边路径同名但结构不同，只能按头分流；各注册一个路由会有一个永远收不到请求）
  - 另提供 `/v1/messages/count_tokens`（按字符数粗估——拿不到上游分词器，
    宁可高估留余量，也不低估导致真实请求超限）

  > 与「转发热路径原样透传」不冲突：本层只改变**回给客户端的表达形式**，
  > 不改动转发给上游的内容。`test_gateway_passthrough` 已相应标注这一例外。

---

## [1.0.35] - 2026-09-15

### 新增
- **API 密钥可以限定版本了——国内版密钥只能调国内版，国际版密钥只能调国际版**：
  新建密钥时选「仅国内版 / 仅国际版 / 不限制」，默认跟随你当前所在的版本。

  此前密钥只有一份**模型白名单**，要手工往里填 `global:` 前缀的模型名才能起到
  隔离作用——不填就等于两版都能调，而界面上并不会提示这一点。现在版本归属是
  密钥自己的属性，由网关强制执行：

  - 用国内版密钥调国际版模型（或反过来）→ 直接拒绝，并说明该改哪里；
  - **调 `/v1/models` 时也按版本裁剪**——限定国际版的密钥只列出国际版模型，
    不会挑出一个注定失败的；
  - 版本判定与实际路由同源（都看模型名的 `global:` 前缀），因此和真实流量一致；
  - **不带 model 的请求会被拒绝**：那会走上游默认模型（国内版），否则限定国际版
    的密钥只要不发 model 就能打到国内账号池，隔离等于没有。

  > **老密钥不受影响**：本功能上线前创建的密钥保持「不限制」，不会因为升级就被
  > 限死（那会让线上正在跑的调用突然报 403）。它们在密钥列表里标为「未限定」，
  > 编辑一次即可选定版本。

### 修复
- **仪表盘的数字把两个版本混在一起算了**：首页「今日 Token / 请求数」与「近 14 天
  调用趋势」取的是**全局**数据，切到国际版也照样显示国内版加国际版的总和——而
  用量统计页早已按版本分开。同一份数据在两个页面对不上，容易让人以为统计坏了。
  现已统一：仪表盘跟随右上角的版本切换，并注明当前显示的是哪个版本。

- **国际版模型列表漏掉了国际版实际可用的模型**（含 `deepseek-v4.1-flash`）：
  模型清单取自腾讯的同一接口，但**国内版与国际版的取法并不相同**——国内版按
  接口里的 `cli` 清单过滤，国际版则是全量。此前把国内版的口径套到了国际版上，
  后果是国际版能用的模型只要不在那份 `cli` 清单里，就从模型中心消失，看起来
  像是「国际版没有这个模型」。现已按两边各自的口径分别取数。

  > 同一个错误也影响另一处：国内版还会剔除非对话模型（嵌入、补全、代码专用、
   > 输出上限过小、图片生成——挑中它们聊天必然报错），这套规则同样源自国内版，
   > 此前一并套在了国际版上。现在只对国内版生效。

- **国际版模型列表在接口返回「简表」时整页空白**：该接口有两种返回形态，一种是
  带完整信息的对象列表，另一种是只有模型名的字符串列表。此前只认前者，遇到后者
  会当作失败处理（页面上表现为取不到模型、并留下一条失败记录）。现已兼容。

### 改进
- **设置页「可用模型」的来源说明不再提「回退到内置静态表」**：上游已改为纯动态
  取数（取不到就是空列表，不再回退到写死在程序里的固定名单）。界面文案已同步；
  旧版上游的那条提示仍在，只是注明它只可能出现在较旧的上游版本上。

---

## [1.0.34] - 2026-09-15

### 新增
- **模型中心显示积分倍率，并可按倍率排序**：模型列表新增**积分倍率**（如 `x0.05`）、
  模型描述、厂商、以及「仅推理 / 默认模型」等标记。同一请求在不同模型上的扣费倍率
  不同，倍率越低越省积分——点「积分倍率 ↑」即可从低到高排，挑最省的模型。
  > 这些字段来自腾讯模型接口。上游这次也补齐了它的 `/v1/models`，两边口径一致，
  > 我们直连取的字段更全（含中文描述）。

### 新增
- **英文版说明文档**：新增 [README.en.md](README.en.md)，顶部可一键切换中英文。
  内容与中文版逐节对应（13 张截图、全部示例代码一致），并已接入发布链路
  —— 发布包、一键更新、安装脚本都会带上它，不会出现「更新完只有中文是新的」。

### 修复
- **容器部署下「更新上游」不可用（判断修正）**：上一版把「容器里不能更新上游」
  当成了不可协商的安全取舍，理由是「挂载 docker 套接字等于把宿主权限交给容器」。
  **这个理由不成立**：宿主部署时本服务本来就是以 root 运行的，而 root 进程本来
  就能挂载宿主根目录——两者权限等价。所以挂载套接字只是让容器版与宿主版能力
  对齐，并没有引入新的风险等级。

  现在容器版与宿主版**能力一致**：能更新上游、能自动重载配置、能读上游日志。
  对应的支持是——镜像内装 docker 命令行、compose 默认挂载 docker 套接字与上游
  仓库目录。若你确实要最小权限，把套接字那行注释掉即可：依赖它的功能会**自动
  降级为「请到宿主机操作」**并在界面提示（不会静默失败）。

### 修复
- **发布包漏打包 Docker 部署文件**：v1.0.33 的压缩包里缺少 `Dockerfile` 与
  `docker-compose.yml`（打包步骤只复制了程序与文档），导致下载发布包后无法
  直接用容器部署，得回仓库另取这两个文件。已补入打包列表。
  > 只影响「下载压缩包做容器部署」这一种用法：容器镜像本身正常（GHCR 可直接
  > `docker pull`），一键更新也不受影响（它只替换程序代码）。

---

## [1.0.33] - 2026-09-15

### 新增
- **支持 Docker 部署**：仓库现在自带 `Dockerfile` 与 `docker-compose.yml`，
  适合已经用 Docker 跑上游的用户；发版时会自动构建镜像推到 GHCR，可直接
  `docker pull`。容器里有三处与宿主机安装不同的**边界**（界面上会如实提示，
  不让人点了才发现）：

  - **不能在界面更新上游**：重建上游容器需要 docker 命令，而挂载 docker 套接字
    会把宿主权限交给容器内进程——风险远大于这点便利，因此刻意不做。请到宿主机
    执行 `docker compose up -d --build`。
  - **更新管理端会重启整个容器**：容器无法重启自己，更新流程是「替换代码 →
    结束容器 → 由 compose 的 `restart` 策略用新代码拉起」，所以 compose 里
    必须保留 `restart: unless-stopped`。
  - **端口默认只绑定本机**：管理端持有全部账号凭据，应藏在反向代理之后。
  - **改完设置需要到宿主机重启一次上游**：上游只在启动时读配置文件，改完必须
    重启它才生效。宿主部署时管理端能直接重启上游容器，但容器里没有 docker 命令
    （刻意不挂 docker 套接字），所以这一步要你手动做——保存设置后界面会提示。

### 修复
- **模型中心不显示推理档位（上游已支持多档强度的模型显示为「不支持」）**：例如
  `deepseek-v4.1-flash`，上游已支持 low/high/max 三档，页面上却是空的。原因是
  推理档位此前只依赖接口返回，而接口对部分模型不给这个字段。现已按上游的做法
  补齐：接口给了就用手上的，没给则查一份产品级档位表（**按国内/国际分开**——
  同一模型在两个版本的档位可能不同，例如该模型国内版三档、国际版仅 high）。

- **仪表盘「反代上游」面板的健康账号/冷却中/已禁用一直显示 0**：它读的是账号
  明细里的 healthy 字段，而**上游的账号明细里根本没有这个字段**——那是 `/status`
  响应**汇总层**才有的（`{"healthy": N, "cooling": N, ...}`），所以布尔转换恒为
  假，三项永远是 0。上游其实早就按版本分好了组（`realm_totals.cn` / `.global`），
  现已直接采用：数字与上游状态机口径完全一致，切换版本时也各算各的。

- **用量统计与请求日志切到国际版后仍显示国内的数据**：顶部切版本此前只影响了
  一部分页面，统计页还明确写着「含两种版本，不按版本过滤」——两个版本走的是
  不同账号池，混在一起看没有意义。现已按版本切分：
  - 「用量统计」的今日/本周请求、Token、实付、按模型、按密钥、趋势图都随版本变化
  - 「请求日志」列表也随版本过滤
  - 数据层补了版本归属（按模型名的 `global:` 前缀判定，与上游的路由协议一致）；
    升级时自动加列，历史记录归入国内版（它们本来就是单一版本的调用）

- **密钥的版本隔离现在有明确入口**：密钥本身按设计**不绑版本**——同一把密钥
  调哪个版本由**请求的模型名**决定（上游的路由协议如此）。要限定某把密钥只能
  用国际版，就在它的「模型白名单」里只列 `global:` 开头的模型；留空则两版都
  可用。密钥编辑弹窗已直接写明这条，不用再猜。

---

## [1.0.32] - 2026-09-15

### 改进
- **冷却中区分「被限流」与「该账号没有这个模型」**：账号冷却时，鼠标悬停会
  列出涉及的模型。但这两类原因的含义完全不同——**被限流**等一会儿会自动恢复；
  **该账号没有这个模型**则等多久都不会好（应该换个模型）。此前混在一起统称
  「被限流的模型」，会让人对着一个永远不会恢复的模型干等。

### 修复
- **账号的设备标识头补齐（继续贴近官方客户端形态）**：官方客户端会为每个账号
  带一个固定的「设备标识」，上游据此识别；缺失或漂移都可能被判定为异常流量。
  账号登录、签到、查积分、地区注册、试用包领取、连通性测试这些操作现已补上
  对应标识（按账号稳定派生，跨重启不变）。

- **「关于」里显示的版本号一直是 1.0.0（不会跟着更新变）**：它读的是前端
  包里一个手写的版本字段，而那个字段从建项目起就没再维护过。后果是**你刚
  更新完，看到的还是 1.0.0**，容易误以为更新没生效。现在改为显示后端实际
  运行的版本号，更新后即随之变化。
  - 同一行的「Build At」也是写死的日期（一直是 2026-09-11），现已改为构建时
    自动记录，反映的是这份前端产物真正打包的时间

- **更新时的 `deploy/` 差异提示现在会说明「要不要紧」**：`deploy/` 里放着
  更新包的验签逻辑，属于信任锚，因此**不随包自动替换**——这本身是设计。
  但此前的提示不论差异是什么都只说「已跳过同步 + 文件名」，于是「新增了一个
  无害的检查脚本」和「验签逻辑被改动」看起来一模一样，没法判断该紧张还是
  该忽略。现在会分开标注：

  - **仅新增** → 明确写「不影响本次更新，可以不处理」
  - **修改普通脚本/配置** → 「看过差异后按需覆盖即可」
  - **修改验签相关文件** → 显式警告，要求人工确认后才覆盖（这是供应链防护
    的最后一关）

---

## [1.0.31] - 2026-09-15

### 安全
- **登录接口遇到异常请求体会报错**：如果请求体是合法 JSON 但不是对象
  （`null`、`[]`、纯字符串、数字），服务端会返回 500（内部错误）。响应本身
  不泄露任何信息，但属于出错而非正常拒绝。现已改为返回「请求格式错误」。

- **修复「Upstash 连通性测试」可被用来探测内网与云元数据（SSRF）**：这个功能
  是把填写的地址交给服务端去请求、再把响应回显，但地址只做了格式归一化、
  没判安全性，因此可以填内网地址或云元数据端点（如 `169.254.169.254`、
  `metadata.tencentyun.com`，后者常能取到服务器临时凭证）让服务端去打。
  现在会拒绝回环、内网、链路本地与已知元数据域名；正常的 Upstash 地址
  （含自建/第三方兼容服务）不受影响。

- **收紧数据库文件权限**：请求日志、审计日志与 API 密钥哈希都存在本地数据库
  里，此前文件权限过宽（同主机的其他用户可读）。现在与用户文件一致，收紧为
  仅属主可读写，含 WAL 模式的两个伴生文件。

### 改进
- **账号「冷却中」现在显示还要等多久**：以前只写「冷却中」，看不出是等几秒还是
  等几小时，只能反复刷新碰运气。现在直接标出剩余时间（如「冷却中 · 12 分钟」）。

- **冷却中会列出被限流的模型**：多个模型同时被限流时，各自的恢复时间并不相同。
  鼠标悬停可以看到具体是哪些模型被限。

---

## [1.0.30] - 2026-09-15

### 修复
- **上游更新完成后，面板仍一直提示「有更新」**：更新检查的结果会缓存 6 小时，
  但更新上游后没有清掉这份缓存，界面就会拿更新前的记录继续比，于是明明已经
  是最新版，却还是一直显示有更新——最长要等 6 小时才自己恢复。现在更新上游
  后立即重新判断。

- **模型中心会列出「选不了」的模型**：嵌入、补全、代码专用、图片生成这几类
  模型（以及输出上限过小的），选中后会直接报错。现在不再列出。

### 改进
- **模型中心显示「默认推理档位」与「多模态」标记**：
  - 推理模型旁标注「默认 xxx」，不用再猜不指定档位时会走哪一档
  - 支持图片输入的模型打「多模态」标，并新增「多模态」筛选按钮

---

## [1.0.28] - 2026-09-14

### 修复
- **账号的「连通性测试」报错看不懂**：报错信息会显示成
  `上游返回 code=11-128 first message is not system prompt` 这样一串英文，
  没有可读解释。现已修复，并给出中文说明。（触发这个报错本身的原因也已修好，
  见下条。）

- **连通性测试失败（报 11-128）**：上一版把系统提示词的默认行为改成「透传
  客户端的 system」后，账号探测请求不再被自动补上 system 首条消息，于是被
  上游拒绝。探测请求现在自带 system，测试恢复正常。

### 改进
- **国际版不再显示签到相关入口**：国际版没有签到体系，此前「全部签到」按钮
  和每个账号的「签到」图标在国际版依然显示，点下去只会得到「已跳过」——现在
  这些入口在国际版不可见，账号页标题也不再提「签到」。
  - 设置页「定时任务」补充说明：签到 / 猫猫旅行 / 活跃上报 / 开学季 / 夜猫
    **只对国内版账号生效**，国际版只有保活照常执行

---

## [1.0.27] - 2026-09-14

### 修复
- **切到国际版添加账号，扫出来的还是国内版二维码**：弹窗标题显示的是国际版，
  但生成的二维码链接指向国内版站点，扫码会加错版本。现已按所选版本生成对应
  的授权链接。

---

## [1.0.26] - 2026-09-14

> **本版有两处默认行为跟着上游一起变了**。若你的用法依赖改动前的效果，
> 需要到设置里手动改回来（下面两条各说明了怎么改）。

### 修复
- **系统提示词默认改为「透传」**：默认不再用网关内置提示词替换客户端发来的
  system，而是原样转发。这是跟随上游的默认值变化。
  - 如果你依赖网关提示词来稳定输出、或想避免模板句被内容审核误判，请到
    **设置 → 系统提示词**把模式改成 `custom`
  - 「自定义提示词文件」只在 `custom` 模式下生效（设置页已注明）

- **出站请求默认对齐官方桌面端**：账号探测等请求现在会带上官方客户端的标识
  头，减少被判定为异常流量的概率。跟随上游的默认值变化。
  - **设置 → 出站标识 → 客户端名称**：留空 = 对齐官方桌面端（新默认）；
    若你之前留空是**有意**想隐藏使用端，请显式填入 `SaaS` 保持原效果

### 新增
- **设置 → 并发与熔断：新增「快过期积分窗口」**：到期时间落在窗口内的积分会
  被标记为「快过期」，选号时优先消耗掉，避免积分白白过期。默认 168 小时
  （一周）；**留空或填 0 即关闭**该优化。

---

## [1.0.25] - 2026-09-14

### 修复
- **减少账号被风控判定为异常的可能**：账号登录、签到、查积分、国际版注册与
  试用包领取这几项操作，请求特征与官方客户端不一致（缺少官方使用的一些标识）。
  现已对齐，降低被风控误伤、进而影响账号的风险。

- **国际版账号的地区归属会填错**：国际版新账号需要先完善注册地区，之前提交的
  地区信息格式不对，可能把归属写歪（表现为聊天报 14017）。现已按正确格式提交。

- **设备风控信息现在会随请求发出**：上游支持用「设备标识」进一步贴近官方
  客户端形态，管理端此前没带。现已支持，并且**重新登录不会把它冲掉**。

- **开学季 / 夜猫任务此前不会真正执行**：这两个任务的脚本之前没被打进上游
  容器镜像，即使到点触发也没有可执行的脚本。上游已修复，更新上游后这两类
  任务会正常出现在「任务记录」里。

---

## [1.0.24] - 2026-09-14

### 安全
- **发布包签名：一键更新强制验签（供应链防护，重要）**：更新器以 root 把
  Release 包直接落盘，而「能发 Release」的门槛比想象中低——能合并 PR 的
  协作者、或被钓鱼的维护者账号都能发版。一旦有人往发布包里塞私货，
  **所有点过更新的部署会被同时控制**（可写 SSH 公钥、定时任务等）。
  现在给发布包加签名，更新器内置公钥、验不过就拒绝安装：
  - 签名由维护者在**本机**用 ssh-keygen 生成（私钥绝不进仓库、也不进 CI，
    因为恶意 PR 能改 CI 步骤把密钥偷走）
  - 更新时先验签、后解压；签名缺失、被篡改、或不匹配都会被中止，包不会落盘
  - 更新器代码所在的 `deploy/` 不再随包自动替换（信任锚不能被更新流程改写），
    如需更新会在日志中提示你手动比对
  - 更新面板显示「已验签」绿色标记；走了跳过开关（`WB_SKIP_SIGNATURE=1`）
    时显示醒目告警
  - 发版签名步骤见 [docs/release-signing.md](docs/release-signing.md)
  > **首次启用需要手动更新一次**：当前已部署的实例还没有验签能力，
  > 请手动更新到本版本；此后的一键更新都会自动验签。

- **修复更新包可写出目标目录之外（严重）**：一键更新会把发布包解压到临时
  目录，但此前的校验存在绕过，恶意的更新包能把文件写到目录**之外**——
  实测可覆盖 SSH 授权文件从而登录你的服务器。现已改为拒绝一切链接成员、
  用严格的路径包含判断，并清掉包内携带的特殊权限位。
  > 触发前提是**更新包的来源被控制**（例如仓库发布权限被窃取）。平时不发作，
  > 一旦发作则是**所有执行过更新的部署同时中招**，故按最高优先级修。

- **修复来源 IP 可被伪造（高）**：服务直接暴露、未经过反向代理时，请求方
  伪造一个来源地址就能绕过**全部 IP 管控**——IP 白/黑名单、密钥的 IP 白名单
  与数量限制、以及登录失败的锁定统统失效。现已只在请求确实来自受信代理时才
  采信转发地址。反代在别的机器上时，用 `WB_TRUSTED_PROXY_CIDRS` 加上它的网段。

- **修复密钥功能不可用（v1.0.23 引入的回归，中）**：上一版给密钥操作加审计时
  漏了一个导入，导致**创建与删除密钥直接报错**。已修复并补测试。

- **`days` 参数加上限**：传入超大数值会让日志/统计接口报错（500），现已钳制。

### 新增
- **新增两类定时任务：开学季与夜猫**（设置 → 定时任务）：上游把这两类从
  宿主机的系统 crontab 迁入了内置调度器，现在可在页面上开关并设置执行时刻
  （默认开学季 12:00、夜猫 01:00）。迁移后不再依赖系统 cron，容器重建也不会丢
  - 它们的执行结果会出现在「任务记录」里（此前日志格式不同，识别不到）

### 修复
- **仪表盘切换版本后仍显示另一版本的数据**：账号总数、积分余额、账号健康快照
  都取自完整账号池，没跟着切换过滤（上一版只给账号页/模型页/任务页做了过滤，
  漏了仪表盘）。现在这些数字都随版本变化，「反代上游」面板里的健康/冷却/禁用
  计数也按版本重算
  - 调用趋势与「今日 Token」是**全局记录**（不按版本拆分），已如实标注
    「含两种版本」，而不是假装已过滤
- **国际版说明写错了**：之前页面上写「国际版没有签到 / 猫猫旅行 / 活跃上报」，
  但上游已放开国际版的**活跃上报**。现改为准确表述：
  国际版没有签到 / 猫猫旅行 / 开学季 / 夜猫，**保活与活跃上报照常执行**
- **账号被禁用时看不出原因**：上游对 `11140 request illegal` 是**硬禁用**
  （到期也不自愈，必须重新授权登录），此前界面统一只显示「已禁用」，
  会让人误以为等一会就好。现在显示「已禁用（需重新登录）」并悬浮提示原因

---

## [1.0.23] - 2026-09-14

### 安全
- **修复管理端提权漏洞（务必尽快升级）**：旧版本的管理接口额外接受一种
  密钥头（`X-API-Key`）作为登录凭据，而这类密钥一旦泄露，就能**直接获得
  管理员权限**——可改密码、加账号、改配置。已彻底移除，管理端只认登录会话。
  > 已泄露的密钥仍写在 `users.json` 里，升级后请按下方「升级后必做」清理。

- **改密码 / 改角色 / 删用户后，相关登录态立即失效**：原先要等到会话自然
  过期（默认 7 天）才失效。现在改动即刻生效，其他用户不受影响。
- **删掉的用户不能再登录**：原先其会话在过期前仍然可用。
- **权限变更立即生效**：原先降权后，旧会话仍按原权限工作。
- **登录失败次数无法再被清除**：攻击者可用大量垃圾账号刷接口，把失败计数
  清空后再暴力猜密码。现已封堵。
- **账号文件损坏时不再自动重置**：原先 `users.json` 读写异常会自动重建，
  导致全部账号与登录态被清空。现在会保留原文件并报错，便于从备份恢复。
- **补充审计日志**：记录登录成功/失败、改密码、增删用户、改安全配置、
  改上游配置、密钥创建与删除，含来源 IP。可在「安全」页查看。
- 其他加固：模型白名单无法再被绕过；接口不再暴露账号池规模；日志无法被
  伪造换行；凭据类操作限管理员。

### 升级后必做（顺序不可颠倒）
1. 在部署目录执行 `python3 deploy/purge_credentials.py`
   —— 立即让**所有**既有登录态失效（含攻击者伪造的），并清掉遗留密钥
2. 重启服务：`systemctl restart workbuddy-web`
3. 用当前密码重新登录，然后**修改管理员密码**
4. 到「设置 → 管理用户」核对账号列表，删掉不是你创建的账号
5. 到「安全」页查看审计日志，确认有无异常登录

### 新增
- **国内版 / 国际版切换**：页面右上角一键切换，全站跟随（账号、模型、测试台、
  任务记录），「添加账号」也会走对应版本。请求日志与用量是全局记录，不随切换过滤
- **国际版支持**：可直接扫码添加国际版（workbuddy.ai）账号，弹窗里选择地区
  完成注册并领取试用额度。
  > 国际版**没有**签到 / 猫猫旅行 / 活跃上报，积分仅来自一次性试用——
  > 这是上游的能力边界，页面上已注明
- **设置页新增「国际版」分组**：可开关国际版路由、自定义其接口地址

### 修复
- 「刷新积分」与单账号积分查询改为仅管理员可用（会真实调用上游，此前任意
  登录用户都能触发）

### 说明
- 升级后**既有账号行为完全不变**（没有标记版本的账号一律按国内版处理）

## [1.0.22] - 2026-09-14
### 安全
- **修复任意文件读取漏洞（严重，务必升级）**：无需登录，构造特制网址即可读取
  服务器上的任意文件——包括 `users.json`（内含登录凭据签名密钥，可据此伪造
  管理员登录）、`.env`、上游配置（含 API 密钥）与账号令牌文件。
  现已封堵，所有静态文件请求都会被限制在网站目录内。

- 账号文件读取接口收紧为白名单（只接受本站自己的账号文件）
- `users.json` 权限收紧为仅属主可读写

### 说明
- 登录会话的签名机制本身没有问题（实测无法通过篡改内容伪造成管理员）；
  失守的原因是密钥文件被上述漏洞读走

## [1.0.21] - 2026-09-14

### 新增
- **模型中心**（底栏「模型」）：把账号可用的模型单列一页，显示名称、上下文
  长度、最大输出与支持的推理档位，支持搜索与筛选
  > 数据取自腾讯接口，因此有**模型全名**和**推理档位**（上游接口不提供这两项）；
  > 取不到时会明确标注来源，不会拿旧数据充数
- **聊天测试台**（底栏「测试台」）：不用先建密钥就能直接试调模型，可选模型与
  思考强度（按该模型实际支持的档位），流式输出、可随时停止，
  每条回答下方标注消耗的积分，右下角显示本次累计
  > 会真实消耗积分，因此仅管理员可用

### 修复
- **任务页筛选后一片空白**：点「余额查询 / 令牌保活 / 自动签到」等没有记录的
  类型后，列表与筛选栏会一起消失，只能刷新页面。现在筛选栏始终保留，
  并在每个类型上直接标出条数（含 0），点之前就知道有没有内容
- **任务页数字自相矛盾**：顶部显示「共 62 条」而列表为空、底部又写「共 0 条」。
  现在所有数字与当前筛选保持一致
- **签到记录看不到上游自动签到**：原先只显示本端触发的手动签到。
  现在本端与上游自动签到合并展示，来源标注清楚
- **测试台点「模型 / 思考强度」没有下拉菜单**：菜单被容器裁掉，看起来像没反应。
  已修复

### 说明
- 上游对签到成功是静默的（只打一行汇总），因此自动签到侧展示的是每轮汇总与
  异常记录，不是逐账号明细

## [1.0.20] - 2026-09-13

### 新增
- **可用模型支持手动重新拉取，并如实标注来源**（设置 → 上游配置 → 可用模型）：
  上游 `/v1/models` 优先用池里**随机一个健康账号**去动态拉取（成功缓存 1 小时），
  **失败则回退到编译进二进制的静态表**（另有 5 分钟负缓存）。这带来两个困惑，
  这次都处理掉：
  - 加「重新拉取」按钮：不必再干等上游那 1 小时缓存，也不会因为「刷新页面」
    看着没变化而以为坏了
  - 来源如实标注：动态时显示「上游实时列表 · N 个」，回退时显示
    **「上游内置回退表（非实时） · N 个」**并给出解释——此前一律写
    「来自上游实时列表」，回退时用户会误以为是自己账号/配置出了问题
  - 判据用上游动态条目才有的 `max_output_tokens` 字段；判不出时不猜，
    回退中性文案（`unknown`）
- **说明模型数量为何因人而异**：列表由上游随机挑一个账号拉取，**取决于该
  账号的授权**（企业 / 套餐），所以「同样的部署、不同账号看到的模型数量不同」
  是上游的正常行为，不是本端少显示。此前有用户反馈「只显示 6 个模型」，
  对照上游源码确认静态表固定为 10 个、其列表是你 16 个的真子集，
  即确为账号授权差异，而非回退或本端缺陷

### 修复
- **「重新拉取」按钮不该被「配置文件可读性」禁用**：模型列表与 config.json
  是否可读是两件事（前者取自上游 `/v1/models`），原先两者耦合会导致
  配置读不到时按钮点不动——恰恰是最需要重试的时候。现只在请求进行中禁用

---

## [1.0.19] - 2026-09-13

### 新增
- **账号列表显示「最后续期」时间**，用来区分「刚被保活续期」和「从没刷新过」：
  界面上那行「有效期」是**剩余时间**，而刷新会把它重新拉满，所以单看天数会
  读反——`7.0 天` 的账号可能刚刚续期、非常健康；`60 天` 的账号反而可能是
  当初扫码签发后**再没被刷新过**，保活没覆盖到，到期就会掉线。
  现在在进度条下方显示「最后续期 N 前」（取 JWT 的 `iat`；刷新会换发新令牌，
  故 iat ≈ 最近一次刷新时刻）。实测三种账号一眼可辨：
  - 刚续期：`7.0 天` + `最后续期 1 小时前`
  - 从未刷新：`5.0 天` + `最后续期 55 天前` ← 真正要留意的
  - 非 JWT：靠文件时间兜底，见下条

### 修复
- **进度条基准的兜底窗口从「一律 60 天」改为按实际推算**：JWT 解不出时
  （扁平形手写 auth 文件的 accessToken 常不是标准 JWT），原先直接假定
  60 天当满格。于是**一个 7 天的令牌进度条只显示 12%**，看着像快过期，
  属于误报。现改为用 auth 文件的 `mtime` 推算——上游刷新 token 后会
  **原子写回**该文件，安装器写盘也走同一路径，故 `exp - mtime` 即本次
  有效期的近似值。实测该场景下进度条从 8.3% 修正为 71.4%。
  依然解不出时保持 `None` 并回退旧的保守窗口，绝不显示负数或误报。

### 说明
- 「最后续期」纯展示、不落库：签发时间从令牌自身解出，刷新换发令牌时会
  自动更新，不需要额外存储，也不会与令牌不一致

---

## [1.0.18] - 2026-09-13

### 新增
- **请求日志新增「首字」列（time-to-first-token）**：原先只有一列「延迟」，
  但那是**整条响应**的耗时——流式请求要等模型把全部内容生成完才结束，所以
  回答越长数字越大，13~24 秒很常见，根本看不出上游响应快慢。
  现在拆成两列：
  - **首字**：从发起上游请求到收到**第一个含正文的 delta**。这才是
    「上游多久开始回话」，判断上游快慢只看它
  - **总耗时**（原「延迟」改名）：含模型生成全程，用来看一条请求的整体开销
  - 非流式请求没有中间过程，首字显示 `—`；详情面板写「未采集（非流式请求）」
  - 阈值也移到首字上：≥3 秒才标琥珀色（总耗时对长回答天然就高，标色只会误报）

### 说明
- 首字采集刻意**不把「收到第一块 SSE」当作首字**：OpenAI 流的第一块通常只有
  `role`、`content` 为空，若以它计时，记的是建连时间，数字偏小且失真。因此
  要等到真正含 `content`（或推理模型的 `reasoning_content`）的 delta 才算
  ——这条已写成回归测试锁定
- 数据库新增 `first_token_ms` 列（可空）。已有部署升级时自动 `ALTER TABLE`
  补列，历史记录该列为 `NULL`（显示 `—`），不会误报为 0ms

---

## [1.0.17] - 2026-09-13

### 修复
- **升级后「更新日志」页报「未找到更新日志文件」**：v1.0.16 新增的更新日志页
  只读部署根目录的 `CHANGELOG.md`，但**更新器与安装脚本都不会把该文件放进
  部署目录**——更新器只替换 `server/`、`web/out/`、`deploy/` 与 `.version`，
  安装脚本也只同步这些。于是发布包里虽然有它，装到机器上却没有，页面直接报错。
  更麻烦的是：已经升到 v1.0.16 的部署因为「已是最新版」不会再触发更新，
  点多少次更新都补不上这个文件。现改为：
  - 解析时按优先级找 `根目录/CHANGELOG.md` → `server/CHANGELOG.md`
  - 发布打包时在 `server/CHANGELOG.md` 放一份副本——`server/` 每次更新都会被
    整体替换，这条路**在任何部署形态下都拿得到**
  - 安装脚本同时把 `CHANGELOG.md`、`README.md` 复制到部署目录
  - 报错信息列出已尝试的路径，便于自行排查
- 顺带说明：v1.0.16 里「更新时同步 CHANGELOG.md/README.md」的改动本身没错，
  但它只在**下一次**更新时才生效，救不了当时已经装上的那份；这次的兜底
  副本才是真正覆盖所有情况的做法

---

## [1.0.16] - 2026-09-13

### 安全
- **修复凭据泄露：上游新增的 `upstream.device_token` 会被明文返回给前端**。
  上游本次新增了设备风控 token（X-Device-Token），它相当于把一台可信设备的
  身份借出去，泄露可被他人复用。而本端的配置接口此前只脱敏了 `api_key` 与
  `upstash.token`，新字段被原样下发（实测确认）。
  现与 Upstash token 同样处理：只回传「是否已配置」+ 掩码，前端拿不到明文
- 写入语义区分清楚，避免把凭据误清空或写坏：
  - **留空** = 保持原值（前端回显的是掩码，不能被当成清空）
  - **非空值** = 设为该值
  - **显式 `null`** = 清除该键（否则没有办法删掉它）
  - 拒绝含换行/控制字符、超过 512 字符的值

### 新增
- **更新提示现在能看出「这批更新做了什么」**：原先只显示上游最新一条提交，
  而上游常一次累积多个提交——只显示一条会让人以为「就改了这一处」，
  也判断不出是功能还是修复、值不值得跟。现在：
  - 显示**落后多少个提交**（如「落后 7 个提交」）
  - 可展开查看这批判的**逐条提交说明**（最多 20 条，最新的在前）
  - 提交前缀（`feat` / `fix` / `refactor`）本身就是分类，一眼可辨性质
  - 用 GitHub compare API 取，本地提交不在远端（上游 force-push）时静默降级，
    不影响版本检测本身
- **出站标识配置补全**（设置 → 上游配置 → 出站标识）：上游本次新增了几个可配项，
  本端一并可视化，便于对齐官方客户端、让官网「使用端」显示正确：
  - `upstream.client_name` —— 用量归属头（X-Product / X-IDE-Name / X-IDE-Type）
  - `upstream.client_version` / `upstream.cli_version` —— 出站 UA 的版本段
  - `upstream.device_token_file` —— 设备 token 文件路径（留空不读）
  - `upstream.passthrough_ip` —— 是否透传客户端 IP（默认关闭，不把内网 IP 暴露给上游）
  - 这些字段均做了控制字符与长度校验；同段其他键不受影响
- **管理端内置「更新日志」**（设置 → 更新日志）：此前只有仓库里有一份
  `CHANGELOG.md`，要关掉面板、打开 GitHub 才看得到更新了什么。现在：
  - 直接渲染 `CHANGELOG.md`，**离线可用**（文件随发布包分发，不需要联网）
  - 按版本折叠，默认展开最新一个；当前运行版本标注「当前版本」并可定位
  - 未发布内容单独标为「开发中」，与已发布版本区分开
  - 分类（安全 / 新增 / 修复 / 改进）带配色标签，一眼看出改动性质
  - 不引入 markdown 依赖：解析器只认本项目固定的三级结构，
    行内仅处理 `**加粗**` 与 `` `代码` ``，发布包更小
  - 顺带修正一个隐患：一键更新原先只替换 `server/`、`web/out/` 与
    `.version`，**不含 `CHANGELOG.md`/`README.md`**——若不同步，
    更新后界面里的日志会一直停在旧版内容。现一并同步

---

## [1.0.15] - 2026-09-13

### 修复
- **反代调用报「请求体过大（上限 8 MiB）」，但上游上限其实可调**：
  网关把上限**写死** 8 MiB（注释说「与上游对齐」，可上游的
  `server.max_body_mb` 是可配置的，管理端设置页也能改）。于是它成了隐性瓶颈——
  把上游上限调大后，请求仍在本端先被 413 拦掉，且看不出是谁拦的。
  现改为**跟随上游 `server.max_body_mb`**（默认 8 MiB；读不到配置时回退默认，
  不因读不到而拒绝服务），加 10 秒缓存避免每请求读文件
- **大小校验原先只看 `Content-Length`**：分块传输时该头缺失、也可以伪造，
  因此超大请求体仍会被整段读进内存（正是当初要防的内存耗尽）。现改为
  **读取时逐块累计并即时截断**，缺头与伪造头都拦得住
- 413 的报错改为可直接照做：说明当前上限、怎么压缩、以及去
  「设置 → 上游配置 → 请求上限」（`server.max_body_mb`）调大

---

## [1.0.14] - 2026-09-13

### 修复
- **手机上密钥的复制按钮被挤出弹窗、点不到**：密钥展示用了
  `flex-1 + overflow-x-auto` 但没加 `min-w-0`——flex 子项默认 `min-width:auto`
  不肯收缩，长密钥就把复制按钮推到弹窗外面。现改为可换行显示
  （`min-w-0 break-all`），复制按钮带文字标签且固定在弹窗内
- **添加账号的授权链接无法复制**：弹窗里只有可点击的链接，想把它发给朋友
  让对方扫码授权时没法复制。现新增「复制链接」按钮，并在支持 Web Share API
  的浏览器（多为手机）额外提供「分享」按钮，可直接转发到微信等应用
- **设置页「服务信息」的长路径会溢出卡片**：`truncate` 同样因缺 `min-w-0`
  不生效（账号目录/上游声明目录）。现补上收缩约束，并给账号目录加复制入口

### 改进
- 新增通用 `CopyButton` 组件统一复制交互：自身 `shrink-0` 不被长文本挤掉、
  移动端触控区域 32×32、复制后短暂显示对勾（移动端看不到 toast 时也明确），
  并在日志详情的来源 IP / User-Agent / 错误等长字段上补齐复制入口

---

---

## [1.0.13] - 2026-09-13

### 修复
- **上游更新失败时能看懂原因，并且有退路**：上游提交 `4a80249f` 删掉了一批
  脚本却漏改 `Dockerfile`（仍 COPY 已删除的 `scripts/probe_active.py`），
  导致 `docker compose up --build` 失败——**这是上游自身的问题**，但当时
  界面只报「命令失败」加一串难懂的 checksum 报错，既看不出原因、也没有回退手段。
  现在增强三处：
  - **构建前预检**：解析 `Dockerfile` 的 `COPY`，若引用了仓库中不存在的文件，
    在构建**之前**就明确报出原因并列出缺哪个文件，不再让你对着 docker 的长报错猜
    （只判定确凿情况，跳过阶段拷贝 / 通配符 / URL，不会误报把正常更新挡下来）
  - **失败诊断**：把 docker 报错归纳成一句人话（Dockerfile 缺文件 / 磁盘不足 /
    网络问题 / 权限不足）
  - **说明当前服务状态**：构建失败时 compose 不会动已在运行的容器，
    日志里会明确写出「旧容器未被影响，服务正常」，避免误以为服务已挂

### 新增
- **固定上游版本**（设置 → 系统更新）：可填提交号或标签，之后「更新上游」
  会检出该版本而不跟随分支，用于上游某提交有问题时**一键回退**；
  留空即恢复跟随分支。版本值经严格校验后才会拼进 git 参数

### 改进
- **任务记录改为「固定高度 + 内部滚动」，不再分页**：分页虽能控制高度，
  但与右侧「上游原始日志」的滚动方式不一致，翻页也不如直接滚直观；
  且一页没填满时下方会留一片空白。现三块都用固定高度内滚，页面高度恒定
  （实测 7.5 屏 → 1.3 屏）
  - 单次拉取 200 条（手机上 80 条）以控制 DOM 规模
  - 命中上限时底部明确提示「共 N 条 · 仅显示最近 200 条，更早的请缩小时间范围」，
    不静默截断
  - 两块并排的卡片高度一致（470px），右侧不再有高度不齐的空白
  - **滚动条改为细样式**：浏览器默认滚动条偏宽、带可见轨道，嵌在卡片里很突兀。
    新增 `.scroll-slim`（主题色 30% 透明滑块 + 透明轨道，悬停加深），
    并统一应用到任务记录的三个列表、表格容器与更新日志区
  - **手机上高度按视口自适应**（`calc(100dvh-200px)`）而非套用桌面固定值：
    手机屏更矮，固定高度会明显偏短、卡片下方留一大片空白；
    用 `dvh` 而非 `vh` 是为了在地址栏收起/展开时也算得准

---

## [1.0.12] - 2026-09-13

### 安全
- **升级 Next.js 15.3.8 → 15.5.25**（响应 Issue #2）：15.3.8 落在多个 Next.js
  公告的影响范围内（含 2 个 critical）。npm audit 已从 3 条降至 **0 条**
  - 说明：本项目生产环境是 `output: 'export'` 静态导出，由 FastAPI 托管静态文件，
    **运行时并没有 Next.js 服务端**（无 middleware / route handler / server actions，
    `images.unoptimized: true`），因此那些 SSRF / RCE / DoS 公告在生产上并不适用。
    但公开仓库应当保持依赖干净，且升级成本极低，故一并处理
  - `eslint-config-next` 同步升到 15.5.25
  - 加 `overrides.postcss = 8.5.28`：Next 内置的 postcss 8.4.31 落在公告范围内，
    顶到已修复版本后 audit 归零；已实测构建产物与全部路由正常
  - 未升级到 Next 16：那是破坏性大版本，收益不符合风险

### 新增
- **实际扣费（实付）可视化**：上游 2026-09-13 起在响应末帧的 `usage.credit`
  里返回**本次真实扣费**，管理端现将它与 Token 一起留痕
  - 请求日志新增「实付」列，详情抽屉显示「实际扣费」（`0` 会标注「未计费」）
  - 用量统计新增「今日实付」卡片（附本周合计），按模型 / 按密钥明细表也加了「实付」列
  - **区分「上游未返回」与「扣了 0」**：缺失存 NULL、界面显示 `—`，
    0 才是真的免费。混为一谈会把未知误判成免费号
  - 顺带扩了 `usage_daily.credit`（当日扣费合计），趋势图数据里也有了
- **数据库自动迁移**：`request_logs` / `usage_daily` 都是已存在的表，
  而 SQLite 的 `CREATE TABLE IF NOT EXISTS` **不会给老表补列**。
  新增 `_migrate()`（`PRAGMA table_info` 检测后 `ALTER TABLE`），
  可重复执行、保留旧数据。升级不再需要手工动库

### 修复
- **任务记录页会随记录数无限变长**：两块列表一次把取回的数据全部渲染，
  实测 500 条任务日志 = **19 屏、9000 个 DOM 节点**，历史一多就要一直往下滑。
  现改为**服务端分页**（每页 20），页面高度不再随数据增长
  （同一份数据：19,229px → 1,615px，DOM 9,003 → 741）
- **列表徽章「共 N 条」与实际可见条数不符**：徽章取的是全局统计，而列表只取
  前 500 条，会出现「徽章说 2200 条、实际只能看到 500 条」且更早的记录翻不到。
  现分页总数取**当前筛选下**的计数，可一直翻到最早一条
- **签到记录占掉半屏多**：行高 39px（结果列的 Badge 有内边距）且与「上游原始
  日志」并排对齐，把右侧只有几行内容的卡片一起撑到 924px，右半屏大片空白。
  现压缩签到行高（39 → 29px）、两块改为**各自独立高度**、并做成 12 条/页

### 改进
- **移动端三块整合成一张卡片 + 分段切换**：手机上三块竖着堆叠、各自带表头与
  分页，页面又长又碎（2.6 屏）。现改为顶部分段（签到记录 / 自动任务 / 原始日志），
  一次只显示一块，页面收敛到 1–1.5 屏；桌面端横向空间足够，仍是三块并列
- **移动端每页条数自适应**：手机是卡片列表（一条约 70px，桌面表格行只有一半），
  任务记录在手机上改为 8 条/页，避免一次拉太长
- 任务记录与签到记录都加了**时间范围筛选**（近 24 小时 / 7 / 30 / 90 天），
  切换类型或时间范围会自动回到第 1 页；概览数字也按同一范围统计，避免对不上

---

### 说明
- 扣费字段来自上游，若上游版本较旧（未返回 `usage.credit`），界面统一显示 `—`，
  属于「无数据显示」而非异常

### 计划中
- **海外版（CodeBuddy 国际版）支持**：受上游限制，暂不可用。上游
  `workbuddy2api` 的出站地址硬编码国内域名（`copilot.tencent.com` /
  `www.codebuddy.cn`），且 `chatBase()` / `billingBase()` 不看账号的
  `domain` 字段（该字段只作为 `X-Domain` 请求头发送，是企业标识而非区域标识）。
  需上游先做成按账号/按域选择 base URL，本端再把自实现的登录/签到/余额地址
  改为可配置。见 [Issue #1](https://github.com/ithtelab/workbuddy-manager/issues/1)

## [1.0.11] - 2026-09-13

### 修复
- **适配上游签到健壮性改造（PR #48）后的日志形态**：上游把签到从「只在失败时
  打日志」改成**成功/已签到也打**，并新增两类非账号日志行。原解析器只认
  `<任务> <uid>: <内容>` 一种形态，会导致三处误判：
  - **「今日已签到」被显示成签到失败**：腾讯把重复签到当业务错误返回
    （code 非 0），但语义上是幂等成功。不特判就会在界面上堆出一片红色失败。
    现对齐上游 `IsAlreadyCheckin` 的判定（已签到 / already），归为正常
  - **凭空多出 `done`、`skipped` 两个「账号」**：每轮汇总行
    `checkin done: total=.. ok=..` 与 `scheduled checkin skipped: ..`
    同样符合 `<任务> <单词>:` 的形态，被当成了 uid。现按形态分别处理：
    汇总行解析成「本轮签到完成：共 N 个，成功 x，已签到 y，失败 z，跳过 w」，
    计划跳过记为一条 info，两者账号列均为空
  - **`checkin <uid> refresh|save: <错误>` 被整行丢弃**：这是上游新增的
    阶段失败行，uid 与阶段名之间是空格而非冒号。现单独识别，能正确记到
    对应账号上（属于真实失败，需要被看到）
  - 另修正：账号被禁用（`连续 N 次 12153 session dead — 禁用`）此前因上游
    只标 `WARN` 而降级为「警告」，但语义是账号已不可用，现统一判为失败
  - uid 识别改用**保留字排除表**而非「必须含数字」：uuid 前 8 位是十六进制，
    存在全为 a-f 的可能（约 0.05%/账号），要求含数字会让那种账号的日志被
    静默丢弃

---

## [1.0.10] - 2026-09-12

### 修复
- **用量统计会把同一次调用算成两天（时区口径不一致）**：写入用量用**本地日期**，
  而「回填用量」用的是 SQLite 的 `date(ts,'unixepoch')` 即 **UTC**。在 UTC+8 的
  服务器上，凌晨 00:00–08:00 的调用本地日比 UTC 日早一天，回填时同一次调用
  被同时算进两天，造成请求数与 Token **重复计数**。
  - 统一取日口径（`db.day_of` / `db.day_sql`，一律本地时区），并提供
    7 项回归测试锁住
  - 新增「**重建统计**」（用量页，管理员）：以请求日志为准重建 `usage_daily`，
    用于清理已被污染的历史数据。回填（增量、只能补缺口）删不掉多出来的行，
    所以需要一次重建；界面上明确提示「若请求日志曾被清空，那部分汇总会丢失」
- **配置页的数字上限形同虚设**：`fieldError`/`toWire` 只校验了时刻、时长与文本，
  `kind: 'num'` 的 `min`/`max` **从未参与校验**，输入框上的 max 只是浏览器属性，
  拦不住手输也拦不住直接调接口。实测「每次上报条数」可写入 999 / 100000
  （即每号每天发这么多条对话，直接烧额度）。现补上前后端双重校验：
  前端即时提示并阻止保存，服务端统一区间兜底（含 `activity_report_count` 1–50、
  `max_in_flight` 0–64、`breaker_threshold` 1–100、`max_body_mb` 1–256 等）
- **「全部签到」串行执行，账号多时必然超时**：逐个 `await`，38 个账号串联
  腾讯 RPC 会超过前端 60 秒超时（前端报失败、后端仍在跑，容易重复点击）。
  改为并发执行并限制并发数（默认 5，`WB_CHECKIN_CONCURRENCY` 可调）；
  实测 38 个账号从 11.4s 降到 2.4s
- **密钥有效期在编辑时完全无法修改**：该字段只在「新建」时出现，密钥一旦创建
  就无法续期、也无法取消过期。现编辑时提供「保持不变／从现在起 N 天／永不过期」
  三选一，并显示当前到期时间（默认保持不变，避免打开就保存把有效期重置）
- **有效期进度条恒为满格**：写死 72 小时（3 天）作为满格，而令牌实际有效期约
  60 天，导致「60 天」和「5 天」看起来一样。现从 JWT 的 `iat`/`exp` 解出该令牌
  的真实总时长，按真实比例展示（实测 58.9/60 天 = 98.2%）；解不出时回退到
  60 天兜底窗口，不影响任何鉴权判断

### 改进
- **二维码轮询加防重入**：`setInterval` 每 2 秒发一次轮询且无在途标志，
  单次超过 2 秒就会叠加请求。现在上一轮未返回时跳过本轮
- **安全配置保存失败会回滚**：此前乐观更新后若请求失败，开关仍停在已被点过的
  状态，与实际后端不一致（刷新才暴露）。现在失败即回滚到改动前的值

---

## [1.0.9] - 2026-09-12

### 修复
- **底栏「密钥」入口消失**：底栏把图标分成「分隔线前 / 后」两段渲染，但前段写死了
  `slice(0, 3)`。新增「任务」入口后它变成第 3 项，原第 4 位的「密钥」被静默丢弃
  （第 4 位正是原来的分隔线位置）。现改为**按分隔线实际位置切分**，不再写死数量
- **适配上游日志格式变更（uid 截 8 位 + WARN/ERR 前缀）**：上游 2026-09-12
  起把日志里的 uid 统一截成前 8 位，并给可疑/失败行加 `WARN:` / `ERR:` 前缀。
  - 任务记录的昵称解析改为**按 uid 前缀匹配**（含前缀歧义保护），
    否则更新上游后账号名会全部退回成 uid
  - 解析器识别 `WARN:` / `ERR:` 级别前缀，直接采信上游的级别判定
  - 适配活跃上报改「5 连发」后的 `report N/M ok` / `report N/M: <err>` 日志行，
    并补充中文翻译

### 新增
- **每次上报条数**（`schedule.activity_report_count`，设置 → 上游配置）：
  上游新增该配置，默认 5 条——领养猫前置需 5 次对话，5 条一次刷满；
  填 1 即旧行为（每号每天 1 条点亮连登）。本端做了 >=1 的校验

---

### 修复
- **适配上游日志格式变更（uid 截 8 位 + WARN/ERR 前缀）**：上游 2026-09-12
  起把日志里的 uid 统一截成前 8 位，并给可疑/失败行加 `WARN:` / `ERR:` 前缀。
  - 任务记录的昵称解析改为**按 uid 前缀匹配**（含前缀歧义保护），
    否则更新上游后账号名会全部退回成 uid
  - 解析器识别 `WARN:` / `ERR:` 级别前缀，直接采信上游的级别判定
  - 适配活跃上报改「5 连发」后的 `report N/M ok` / `report N/M: <err>` 日志行，
    并补充中文翻译

### 新增
- **每次上报条数**（`schedule.activity_report_count`，设置 → 上游配置）：
  上游新增该配置，默认 5 条——领养猫前置需 5 次对话，5 条一次刷满；
  填 1 即旧行为（每号每天 1 条点亮连登）。本端做了 >=1 的校验

---

## [1.0.8] - 2026-09-12

### 修复
- **任务结果全是英文，看不懂**：上游日志是英文原文（`claim ok record=12
  reward=100`、`skip (daily limit reached)` 等），直接展示对中文用户不友好。
  现按展示层做中文化（如「领奖成功：第 12 次行程，获得 100 积分」
  「跳过：今日次数已达上限」），**数据库仍保留英文原文**，鼠标悬浮可看原话，
  便于对照上游日志排查；认不出的新文案原样回退，不会丢信息
- **任务记录里的账号显示为 uid 而非昵称**：上游日志只带账号 uid，
  而账号表里是昵称，两边对不上；现按 uid → 昵称映射显示（取不到才回退 uid）
- **「上游签到 / 保活日志」标题与说明过时**：该区块现在也含旅行与活跃日志
  （上一版放开过滤后），标题改为「上游原始日志」，说明也改为
  「签到成功与保活正常是静默的」，并指向结构化的「自动任务与积分记录」

### 改进
- **全面改为心跳刷新，移除各页多余的「刷新」按钮**：所有数据页都按固定间隔
  自动拉取（账号 / 任务 / 仪表盘 30 秒，密钥 / 日志 / 用量 / 安全 60 秒），
  手动刷新按钮因此完全重复 —— 需要时用浏览器自带的刷新即可。
  心跳统一走 `useHeartbeat`：**标签页切到后台时跳过刷新**，切回来时立即补一次，
  避免后台空转请求，也不会回来看到旧数据
- **签到记录与任务记录独立成「任务记录」页**：账号一多，这两块堆在账号管理
  下方会把页面拉得很长，往下翻很久才能看到日志。现在移到独立页面
  （底栏新增「任务」入口），账号页只留账号列表，并在下方给出跳转提示；
  新页面提高展示上限（签到 60 条、任务 500 条）且不再内嵌滚动区，
  一页看全，30 秒自动刷新
- **采集器回看窗口从 800 行加到 3000 行**：上游每处理一个请求就打一行访问日志，
  行数消耗很快；窗口太小可能在两次轮询（45 秒）之间漏掉任务行，
  且刚升级部署时只能补到很短一段历史

---

## [1.0.7] - 2026-09-12

### 新增
- **积分变动流水**：所有让余额增加的渠道都会留痕，不再只有旅行领奖可见
  - 上游只在猫猫旅行领奖时打印具体数额（`reward=100` / `+300 credits`），
    签到、活跃上报等**根本不打日志**，所以光解析日志覆盖不全
  - 现在每次真实查询积分后比对上次余额，**只要增加就记一条**「积分变动」
    流水（含 `余额 +100（1300 → 1400）`），从而覆盖全部获取渠道；
    减少是消耗，不计入
  - 余额基线持久化在数据库，重启后继续比对；首次见到的账号只建基线，
    不会把历史余额误报成「刚获得」
  - 「自动任务与积分记录」新增「积分变动」类型筛选
- **积分数据来源可见**：账号积分旁标注 **`实时`** 或 **`缓存 Ns 前`**，
  一眼看出这个数字是刚查的还是 60 秒内复用的缓存，不再把滞后值当成实时值
- **系统提示词模式可配置**（设置 → 上游配置）：上游新增 `prompt.mode`，
  默认 `custom` 会**用网关自带提示词替换客户端传来的 system 消息**。
  现在可在界面上切换：
  - `custom`（替换为网关提示词，默认）
  - `passthrough`（透传客户端 system，保留你自己的角色设定与格式约束）
  另可指定自定义提示词文件路径（`prompt.file`）。
  该分组带显式警示：`custom` 会覆盖下游 system prompt；填了文件路径就必须可读，
  否则上游启动会直接失败
- **请求体上限**（`server.max_body_mb`）与**出站 User-Agent**（`upstream.user_agent`）
  纳入可视化配置——都是上游本次新增、此前界面缺失的字段
- **自动任务与积分记录**（账号页）：猫猫旅行 / 活跃上报 / 自动签到 / 保活
  的执行结果与**积分收益**现在可以查看，不必再登服务器翻容器日志
  - 上游把这些结果打在容器日志里，且**容器一重建（更新上游）日志就没了**。
    现在由后台采集器每 45 秒解析一次新日志并落库（`task_logs` 表），长期保留
  - 解析出类型、账号、积分、以及结果等级（积分 / 正常 / 跳过 / 告警 / 失败），
    旅行领奖（`reward=100`、`adopt ok (+300 credits)`）会显示为 **+100 / +300**
  - 支持按类型筛选，顶部汇总总条数与**累计积分**；提供「立即采集」按钮
    不必等后台轮询
  - 采集用「时间戳 + 内容」做去重键，重复读取同一行不会重复入库
  - 新增接口 `GET /api/task-logs`、`POST /api/task-logs/collect`、
    `POST /api/task-logs/clear`
- 上游日志过滤补齐 `travel` / `activity`，此前这两类日志在「上游签到 / 保活日志」
  里被直接丢掉（只匹配 checkin / keepalive / user-resource）；
  该区域同时不再显示 docker 的重复时间戳前缀

### 修复
- **打开账号页时积分有时不显示**：积分查询与账号列表是两个并发请求，
  积分常常先返回，此时账号列表还是空的，就地更新会落空。
  现改为按 uid 单独保存、渲染时再合并，与请求先后无关

### 改进
- **移动端 UI**：
  - 账号列表在手机上改为**纵向卡片**（昵称/UID/状态/积分/有效期/操作一屏可见），
    桌面端仍用表格；原先 6 列表格在窄屏需要横向拖动才能看全一行
  - 「自动任务与积分记录」同样在手机上改为卡片
  - 修复设置页标签栏在手机上撑破页面（整页横向溢出到 492px），
    现改为可横向滚动
  - 头部按钮在手机上收敛文案（「强制重启」→「重启」、「刷新积分」→「积分」）
- **移除仪表盘多余的「刷新」按钮**：该页每 30 秒自动刷新，且没有任何会
  改变数据的操作，按钮纯属重复；描述里已注明自动刷新
- **设置页布局紧凑化**：分组内的字段改为宽屏两列（开关与它对应的时刻/数值
  天然成对），八行「定时任务」压到四行；去掉每个字段下方重复的格式说明，
  仅在填错时显示；卡片与标签页间距整体收紧

---

## [1.0.6] - 2026-09-11

### 修复
- **已是最新版本却提示「发现新版本可用」，还显示成降级**
  更新到 v1.0.5 后，界面弹出「管理端：v1.0.5 → v1.0.4」。
  根因是版本比较写成了「不相等」而不是「严格更新」：检测结果缓存 6 小时
  （避免触发 GitHub 未认证 API 限流），缓存里存的 `latest` 是更新前查到的
  v1.0.4，与当前版本 v1.0.5 不相等 → 被当成有更新。
  现在改为**数值化比较且必须严格大于当前版本**——
  `1.0.10 > 1.0.9`（按数值而非字符串）、相等不算更新、
  低于当前版本（回滚场景）也不再提示。
- **更新完不清版本缓存**，导致刚发布的新版本最长 6 小时才被发现。
  现在管理端更新成功后清除 `version-check.json`，下一轮检测立即拉取最新。
  即使在 GitHub API 限流、只能沿用旧缓存的极端情况下，
  「严格大于」的比较也保证不会再冒出降级提示。

---

## [1.0.5] - 2026-09-11

### 修复
- **一键更新「管理端」成功后仍显示「更新未完成」**
  更新脚本最后一步是 `systemctl restart workbuddy-web`，而 systemd 默认
  `KillMode=control-group` 会连同同一个 cgroup 内的更新进程一起终止
  （`start_new_session=True` 只脱离终端会话，并未脱离 service 的 cgroup）。
  旧流程把「写入成功状态」放在重启之后，于是状态文件永远停在
  `running=true, ok=null`，管理端一读就判成「更新进程异常中断」——
  代码其实已经换好了。
  现在改为**先落盘终态再重启**，并让管理端在发现「标记运行中但进程已不在」
  时进一步核实：目标版本已就位、或日志已打印「管理端已更新到 X」且与
  当前版本一致，即判定为成功（`更新完成（服务已重启）`）；两者都不满足
  才算真中断。
- **更新后「当前版本」不刷新，界面一直提示「发现新版本可用」**
  发布包里的 `.version` 标记在更新时没有一并替换，且读取顺序是
  「标记优先于代码」，导致刚更新完仍显示旧版本号。
  现在更新会同步替换标记；读取策略调整为**以代码版本为准**，
  标记与代码不一致时自动纠正（兼容旧流程装出来的部署）。

---

## [1.0.4] - 2026-09-11

### 修复
- **配置页把「执行时刻」当成数字间隔写入，会写坏上游配置**
  `schedule` 下的 `checkin_hours` / `travel_hours` / `activity_hours` /
  `keepalive_hours` 在上游是**整点数组**（如 `[9, 21]`），此前界面按
  「每隔几小时」渲染为数字输入框，保存会写成 `"checkin_hours": 6`，
  既非数组也非整点，重新加载后行为不可预期。
  现改为逗号分隔的时刻输入（`9, 21`），带即时校验（0-23、去重、排序）
- **`pool.breaker_cooldown` 字段类型写错**：上游该字段为时长字符串
  （默认 `30m`），此前按秒数整数渲染，保存会把 `"30m"` 覆盖成数字导致
  上游启动报错。现统一为时长输入并做格式校验
- **「更新完成」提示无法关闭**：更新结束后的结果横幅没有关闭入口，
  会一直占据设置页。现右上角增加关闭按钮；发起下一次更新时会重新出现，
  日志区仍保留完整记录
- **`updater_found` 提示在页面初次加载时闪现**：状态尚未拉取（`null`）
  时被误判为「未找到更新脚本」。现仅在确实读到 `false` 时提示

### 新增
- **定时任务可视化配置**：四类任务（签到 / 猫猫旅行 / 活跃上报 / 保活）
  各自独立的开关与执行时刻，对应上游新增的 `travel_*` / `activity_*` 字段
- **功能开关 / 会话粘性配置**：`features.sanitize_blacklist_fingerprints`、
  `session_sticky`（开关、`ttl`、`gc_interval`）纳入可视化编辑
- **并发与熔断补全**：`idle_weight_per_hour` / `idle_weight_max`
  （闲置补偿，支持小数）可直接调节
- 高级 JSON 编辑器扩展为 5 个段：`schedule` / `cooldown` / `pool` /
  `features` / `session_sticky`
- **配置写入服务端兜底校验**：时刻数组必须是 0-23 的整数数组、
  时长必须是 `30s / 10m / 2h / 1d` 形式，非法值直接拒绝并说明原因，
  不会写入上游配置。新增 10 项回归测试覆盖读写往返

### 改进
- `cooldown.soft_rate` 默认值跟随上游改为 **600s**（此前上游为 60s）

---

## [1.0.3] - 2026-09-12

### 新增
- **一键更新**（设置 → 系统更新）：网页上直接更新，无需登录服务器
  - **新版本自动检测与提醒**：进入页面即检查管理端 Release 与上游最新提交，
    发现更新会弹出提醒，并在面板显示版本对比（当前 → 最新）与上游最新提交说明，
    附针对性的「更新上游 / 更新管理端」按钮
  - 检测结果缓存 6 小时（GitHub 未认证 API 限流较严），支持手动「检测更新」
  - **全部更新 / 仅上游 / 仅管理端** 三种模式，带二次确认
  - 实时进度：更新过程与日志逐行展示（后台执行，页面自动重连）
  - 账号授权、上游配置、密钥与日志数据全部保留
  - 上游更新会**自动重新施加端口收敛**（127.0.0.1），避免上游仓库里的
    `7863:7863` 覆盖安全基线
  - 安全约束：仅管理员可用；目标为固定枚举，路径取自服务端环境，
    **不接受客户端传入命令或路径**；解压做目录穿越校验；并发更新加锁
- 版本标记：发布包与安装脚本写入 `.version`，界面据此显示当前版本

### 修复
- **用量柱状图在数据点少时被拉伸**：未限制柱宽，只有一两天数据时单根柱子
  会占满整个图表。现限制最大宽度（48px）

### 改进
- **部署大幅简化**：`deploy/install.sh` 升级为一键脚本，自动检测并安装上游
  workbuddy2api（克隆、生成随机 api_key、修正目录属主、构建并启动容器、
  等待就绪），随后安装管理端并注册 systemd 服务。解决「clone 本仓库后
  因缺少上游而无法运行」的问题
  - 已存在上游时自动跳过，不覆盖其配置与账号（含 `--skip-upstream` 参数）
  - 支持 `APP_DIR` / `UPSTREAM_DIR` / `MANAGER_PORT` 等环境变量自定义
  - 含环境预检（Python 版本、Docker、端口占用）与非 root 拒绝
- 新增 [deploy/README.md](deploy/README.md)：完整部署指南
  （Release 包与 git clone 两种方式、Nginx 配置示例、加固建议、常见问题）
- 发布包纳入 `docs/`（此前只含顶层文件，导致缺少安全审查报告）

### 计划中
- 出站 IP 池：为每个腾讯账号绑定独立出口代理（需在上游 workbuddy2api 侧支持）
- 用量统计支持小时级粒度

---
## [1.0.2] - 2026-09-12

### 改进
- **部署大幅简化**：`deploy/install.sh` 升级为一键脚本，自动检测并安装上游
  workbuddy2api（克隆、生成随机 api_key、修正目录属主、构建并启动容器、
  等待就绪），随后安装管理端并注册 systemd 服务。解决「clone 本仓库后
  因缺少上游而无法运行」的问题
  - 已存在上游时自动跳过，不覆盖其配置与账号（含 `--skip-upstream` 参数）
  - 支持 `APP_DIR` / `UPSTREAM_DIR` / `MANAGER_PORT` 等环境变量自定义
  - 含环境预检（Python 版本、Docker、端口占用）与非 root 拒绝
- 新增 [deploy/README.md](deploy/README.md)：完整部署指南
  （Release 包与 git clone 两种方式、Nginx 配置示例、加固建议、常见问题）
- 发布包纳入 `docs/`（此前只含顶层文件，导致缺少安全审查报告）

---
## [1.0.1] - 2026-09-12

### 安全（详见 [安全审查报告](docs/SECURITY-AUDIT.md)）

- **修复高危：伪造 `X-Forwarded-For` 可绕过 IP 管控与登录锁定**
  反向代理以「追加」语义写 XFF，首段完全由请求方控制；原先直接取首值，
  导致入站 IP 白/黑名单、每密钥 IP 限制、登录失败锁定均可被冒充绕过。
  现优先采用反代覆盖写入的 `X-Real-IP`，退化时从 XFF 右侧取值
  （新增 `WB_TRUSTED_PROXY_HOPS` 适配前置 CDN）
- **修复高危：登录爆破仅按 IP 计数**，换 IP 即可无限尝试同一账号。
  现改为「IP + 用户名」双维度锁定（各 5 次 / 10 分钟）
- **修复中危：`/docs`、`/redoc`、`/openapi.json` 对公网暴露**，
  把全部管理接口结构告知未认证访问者。生产环境默认关闭
  （`WB_ENABLE_DOCS=1` 可开启）
- **修复中危：`/api/sysinfo` 未认证即泄露服务器绝对路径**。改为需登录，
  且仅返回服务名与版本
- **修复中危：网关无请求体大小限制**，超大请求会整段读入内存。
  现校验 `Content-Length`，超过 8 MiB 返回 413（与上游限制对齐）
- **修复低危：登录失败计数字典无上限**，可被大量源 IP 撑占内存。
  现设容量上限并清理过期项
- **修复低危：网关无限流**，单密钥被打爆会拖累整个账号池。
  现按密钥做滑动窗口限流（默认 120 次/分钟，可配），超出返回 429
- **新增安全响应头**：`X-Content-Type-Options`、`X-Frame-Options: DENY`、
  `Referrer-Policy`、`Permissions-Policy`、CSP

### 修复
- **积分余额不准确 / 不自动更新**：上游 `/status` 的 `credits` 只在上游自己的
  定时任务里刷新，滞后可达数小时，且手动签到不会带动它更新。现改为管理端
  直接向腾讯查询实时余额（带 60 秒缓存，避免频繁请求）：
  - 账号页打开时自动拉取一次实时积分
  - 签到后立即返回刷新后的积分，并就地更新显示
  - 新增「刷新积分」按钮，可手动拉取全部账号的实时余额
  - 单个账号积分接口支持 `force=true` 绕过缓存

### 改进
- 账号页与仪表盘新增 30 秒自动轮询，避免长时间停留时展示陈旧数据
- 图标 / 字体等静态资源改为缓存一天（此前每次回源校验）

---
## [1.0.0] - 2026-09-11

首个正式版本：为 [`workbuddy2api`](https://github.com/Sliverkiss/workbuddy2api)
提供完整的 Web 管理控制台与 OpenAI 兼容反代网关。

### 新增

**账号管理**
- 扫码纳管腾讯账号（微信 / QQ），授权后自动签到、落盘并重载上游
- 账号列表：Token 有效期进度、状态徽章（在线 / 冷却中 / 已禁用 / 已过期）
- **积分余额查看**：显示各账号当前可花费积分，按余额分级着色
  （≤0 红 / <200 琥珀），未同步时显示 `—` 而非误报 0
- 单账号操作：签到、连通性测试、刷新 Token、删除
- **一键全部签到**，逐账号记录结果

**反代网关（对外 `/v1`）**
- OpenAI 兼容协议，支持流式（SSE）与非流式
- 多密钥分发：每把密钥独立设置有效期、最大 IP 数、IP 白名单、
  模型白名单、Token 配额
- 模型别名映射：把下游习惯的模型名映射到实际模型
- 入站 IP 管控：全局白 / 黑名单（支持 CIDR），白名单模式可仅放行可信来源
- 全量审计：记录密钥、来源 IP、模型、状态码、延迟与 Token 消耗

**签到可追溯**
- **签到记录**：记录由本端触发的签到结果（手动 / 批量 / 添加账号），
  失败标红并显示原因
- **上游签到 / 保活日志**：采集容器日志中的 checkin / keepalive 记录

**可视化设置**
- 签到与保活：开关 + 间隔，附白话说明，无需手改 JSON
- 并发与熔断：单账号并发、失败阈值、冷却时间
- **Upstash（Redis 持久化）配置入口**：地址 + Token + 连通性测试，
  多实例部署时可共享会话粘性与账号状态
- 只提交改动项，不会覆盖未展示的配置；冷门参数保留「高级设置」
- **保存后自动重载上游**（约 0.5 秒，多次改动自动合并为一次重启）

**用量与缓存**
- 用量统计：按天 / 模型 / 密钥多维聚合，含趋势图
- **用量统计修复**：可按请求日志回填历史缺口（幂等）
- 缓存策略分级：静态资源长期强缓存、API 禁止缓存、HTML 走 ETag 校验

**访问控制**
- 用户名 + 密码登录，PBKDF2-SHA256 加盐存储
- 角色分级：`admin` 可读写 / `viewer` 只读
- HttpOnly 签名 Cookie 会话，同 IP 连续失败 5 次锁定 10 分钟

**界面**
- 浮动底栏：可拖拽、磁吸放大，以水平中心为锚点（悬停不漂移）
- 语义配色：有效期与积分按业务含义分级着色
- 卡片式通知：弹簧入场、语义色图标、底部倒计时线
- 深浅色主题，跟随系统

### 修复

- **非流式请求返回 502**：用量统计函数被注释吞掉，且统计层异常会中断
  流式响应收尾（表现为内容已输出但报 `terminated`）。现已恢复函数并为
  日志 / 统计分别加异常兜底，旁路故障不再影响用户请求
- **配置保存不生效**：路由为同步函数，FastAPI 在线程池执行时无法调度
  后台重载任务，导致保存后配置从未应用。已改为异步路由
- **连通性测试恒报 `code=11101`**：探测使用了非流式请求，而上游强制要求
  `stream=true`；同时补齐了上游要求的请求头（X-User-Id / X-Product 等）
- **管理端读不到上游状态**（`/api/status` 恒为不可用）：构造超时时同时传入
  `Timeout` 实例与 `connect` 关键字，httpx 直接抛 AssertionError
- **上游配置接口泄露明文凭据**：接口会返回包含 API Key 与 Upstash Token
  的原始配置对象（前端并未使用）。现改为仅返回掩码
- **弹窗双层边框**：半透明背景叠加遮罩形成明暗分界，且 `border` 与 `ring`
  重复。现使用不透明背景与单一边框
- **账号计数偶发显示 0**：`inView` 在弹窗缩放挂载时误判为不可见
- **切换页面闪现「正在校验登录态」**：登录校验阻塞了页面渲染
- **添加账号弹窗授权链接溢出**：flex 子项默认 `min-width: auto` 导致
  `truncate` 失效
- **设置页无法读取上游配置时**：明确提示原因并锁定保存，避免空配置
  覆盖真实文件

### 变更

- 上游配置从裸 JSON 改为中文可视化表单（原 JSON 编辑移入「高级设置」）
- 设置页移除冗余的「立即重启上游」按钮（保存已自动重载）
- 账号页「重启上游」更名为「强制重启」，明确为异常恢复手段
- 站点图标由 LDC 的标识替换为本项目形象

### 安全

- **清除设计文档中的真实凭据**：文档含服务器 SSH 密码、公网 IP、上游
  API Key 与面板入口路径，已替换为占位符，并通过历史重写从全部提交中移除
- 上游 API Key 与 Upstash Token 不再通过接口下发（仅返回掩码）
- 网关密钥仅存 SHA-256 哈希，明文只在创建时返回一次
- 所有文件操作做路径穿越校验
- `.gitignore` 排除 `users.json`、`data/*.db`、`.env`、账号授权文件

---

## 版本说明

- 本项目**不修改** workbuddy2api 的 Go 代码，管理端与其独立部署
- 由于上游仅在进程启动时读取配置与扫描账号目录（且不处理 SIGHUP），
  配置变更与账号增删需要通过重启容器生效，本项目已将其自动化

---

## 版本对比

- [v1.0.1 → v1.0.2](https://github.com/ithtelab/workbuddy-manager/compare/v1.0.1...v1.0.2)
- [v1.0.0 → v1.0.1](https://github.com/ithtelab/workbuddy-manager/compare/v1.0.0...v1.0.1)
- [全部提交](https://github.com/ithtelab/workbuddy-manager/commits/main)
